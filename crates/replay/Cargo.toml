[package]
name = "replay"
description = "The replay format: a recorded game as its seed, rules, handling and inputs."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license = "MIT OR Apache-2.0"

[dependencies]
engine = { path = "../engine" }
serde = { workspace = true, features = ["std"] }

[dev-dependencies]
serde_json = "1"
